---
description: 主代理禁止直接分析与编辑，所有分析与编辑统一委托给单一 fable5xhigh 子代理会话
alwaysApply: true
---

# 子代理独占分析与编辑

## 适用范围

本规则对本工作区内所有对话与任务生效。主代理（当前会话）仅负责与用户沟通、澄清需求、汇总结果；**禁止**自行执行任何代码分析或文件编辑。

## 主代理禁止事项

主代理**不得**直接或间接执行以下操作：

- 阅读、浏览、检索、理解代码或配置（含 `Read`、`Grep`、`Glob`、语义搜索、auto-index 等一切探查手段）
- 修改、创建、删除任何文件（含 `Write`、`StrReplace`、`EditNotebook`、`Delete` 等）
- 运行用于分析或验证代码/构建结果的命令（含编译、测试、lint、git diff 等）
- 自行得出结论、给出补丁、写出具体改动方案或逐文件修改建议

主代理允许的操作仅限于：

- 与用户对话（提问、确认、汇报、转述子代理结果）
- 向子代理发布**模糊指令**
- 使用 `Task` 工具向**唯一**子代理会话发送指令或 `resume` 续聊
- 在子代理返回后，将结果用自然语言整理给用户（不得自行补充未经验证的分析或编辑）

## 子代理委托方式

### 模型

所有分析与编辑**必须**委托给子代理，并**固定**使用模型：

`claude-fable-5-thinking-xhigh`（简称 fable5xhigh）

调用 `Task` 时 `model` 参数必须设为上述值，不得改用其他模型。

### 单一子代理会话（强制）

- 全程**只允许一个**子代理会话。
- **首次**需要分析与编辑时：调用 `Task` 创建子代理，并保存返回的 `agent ID`。
- **之后**所有分析与编辑：必须使用同一 `agent ID` 通过 `resume` 续聊，**禁止**再次调用 `Task` 创建新子代理。
- **禁止**并行启动多个子代理；**禁止**为不同子任务新开子代理会话。

### 模糊指令原则

主代理向子代理下达的指令必须是**模糊指令**，不得包含可直接执行的细节，例如：

- 禁止写明具体文件路径、函数名、行号、代码片段、补丁内容
- 禁止逐步操作清单（「先读 A，再改 B 第 12 行」）
- 禁止指定工具调用顺序或具体命令

模糊指令应只描述**目标、约束与验收标准**，把如何分析、改哪些文件、怎么改全部交给子代理自行决定。示例：

- 可接受：「按用户最新需求把相关功能修好，保持现有测试通过，改完汇报改了什么。」
- 不可接受：「打开 `src/foo.py`，把 `bar()` 里的 `if x` 改成 `if y`。」

主代理从用户处获得的具体信息，应原样或概括后转交给子代理，由子代理自行拆解与执行；主代理不得替子代理完成拆解。

## 工作流

1. 用户提出需求 → 主代理确认理解（不分析代码）
2. 主代理向**唯一** fable5xhigh 子代理发布模糊指令（首次 `Task`，后续 `resume` + 同一 `agent ID`）
3. 子代理完成分析与编辑并返回结果
4. 主代理向用户汇报子代理结论（不自行增删技术分析或改动）

## 冲突处理

若本规则与其他规则在「是否允许主代理直接分析/编辑」或「是否允许新开子代理」上冲突，**以本规则为准**。
